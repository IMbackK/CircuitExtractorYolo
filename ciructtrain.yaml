# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /home/philipp/Programming/kiss/CircutExtractorYOLO/  # dataset root dir
train: labeled  # train images (relative to 'path') 128 images
val: labeled  # val images (relative to 'path') 128 images
test:  # test images (optional)

# Classes
nc: 1  # number of classes
names: [ 'circut' ]  # class names
