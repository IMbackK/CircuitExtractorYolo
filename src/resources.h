#pragma once
#include <string>
#include <vector>

namespace res
{

const char* circutNetwork(size_t& size);
const char* elementNetwork(size_t& size);
const char* graphNetwork(size_t& size);
std::vector<std::string> dictionary();

}
